# @package _global_

name: sdnet_p_factor_exp1
desc: P_factor based on relative area of mask

client:
  lr: 0.0001
  p_factor_enabled: True
  model: sdnet

server:
  num_rounds: 100
  gradient_mode: True

data:
  load_names: True
  my_details: True