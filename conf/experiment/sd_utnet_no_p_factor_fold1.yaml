# @package _global_

name: sd_utnet_no_p_factor_fold1
desc: No p_factor (baseline)

client:
  lr: 0.0001
  p_factor_enabled: False

server:
  num_rounds: 100
  gradient_mode: True

data:
  load_names: True
  out_center: 6
  splits: per_fold
  fold_nr: 1
  my_details: True